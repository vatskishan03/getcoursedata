{
  "name": "courseraproject-with-link",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "npm run build:client && npm run build:server",
    "build:client": "parcel build public/index.html",
    "build:server": "python script.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "body-parser": "^1.19.0",
    "csv-parser": "^3.0.0",
    "python-shell": "*",
    "cors": "^2.8.5"
  }
}
